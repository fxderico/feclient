package dev.fede.water;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/** Stub: WaterClient singleton deleted; only Logger preserved. */
public final class WaterClient {
    public static final Logger a = LoggerFactory.getLogger("feClient/water");
    private WaterClient() {}
}
